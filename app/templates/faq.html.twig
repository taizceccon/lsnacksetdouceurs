{% extends 'base.html.twig' %}

{% block title %}FAQ | Leila Snacks & Douceurs{% endblock %}
{% block meta %}
  <meta name="description" content="A propos de Leila Snacks et Douceurs">
{% endblock %}

{% block body %}

<section class="banner-contact bg-roseFonce dark:bg-roseFonce py-16 sm:py-20 text-center rounded-md mb-12 px-4 sm:px-6">
    <h1 id="contact-heading" class="text-4xl sm:text-5xl font-script text-rose-100 dark:text-[#EDCC8B] mb-4">
        Leila Snacks & Douceurs<
    </h1>
    <p class="max-w-3xl mx-auto text-lg sm:text-xl text-rose-100 dark:text-[#EDCC8B]">
    Des délices sucrés d’exception, pensés pour combler les palais les plus raffinés.<br>
    </p>
</section>

<!-- FAQ Section -->
<section class="max-w-3xl mx-auto px-4 py-8">
  <h2 class="text-2xl font-bold mb-6">FAQ</h2>

  <ul class="space-y-4">
    <li>
      <button data-modal="modal1" class="text-left w-full text-[#7B9E87] hover:underline font-medium">
        Comment passer commande ?
      </button>
    </li>
    <li>
      <button data-modal="modal2" class="text-left w-full text-[#7B9E87] hover:underline font-medium">
        Quels sont les délais pour recuperer la commande ?
      </button>
    </li>
  </ul>
</section>

<!-- Modale 1 -->
<div id="modal1" class="fixed inset-0 bg-black bg-opacity-50 flex items-center justify-center z-50 hidden">
  <div class="bg-white text-gray-800 max-w-md w-full p-6 rounded shadow-lg relative">
    <button class="absolute top-2 right-2 text-gray-500 hover:text-gray-800" data-close>&times;</button>
    <h3 class="text-lg font-semibold mb-2">Comment passer commande ?</h3>
    <p>Créez un compte, ajoutez vos produits au panier, puis validez la commande via le bouton "Commander".</p>
  </div>
</div>

<!-- Modale 2 -->
<div id="modal2" class="fixed inset-0 bg-black bg-opacity-50 flex items-center justify-center z-50 hidden">
  <div class="bg-white text-gray-800 max-w-md w-full p-6 rounded shadow-lg relative">
    <button class="absolute top-2 right-2 text-gray-500 hover:text-gray-800" data-close>&times;</button>
    <h3 class="text-lg font-semibold mb-2">Quels sont les délais pour recuperer la commande ?</h3>
    <p>Les délais pour recuperer la commande varient entre 2 et 5 jours ouvrés.</p>
  </div>
</div>


<script>
  // Ouvrir la modale
  document.querySelectorAll('button[data-modal]').forEach(button => {
    button.addEventListener('click', () => {
      const modalId = button.getAttribute('data-modal');
      const modal = document.getElementById(modalId);
      if (modal) modal.classList.remove('hidden');
    });
  });

  // Fermer la modale
  document.querySelectorAll('[data-close]').forEach(button => {
    button.addEventListener('click', () => {
      button.closest('.fixed').classList.add('hidden');
    });
  });

  // Fermer la modale si on clique en dehors du contenu
  document.querySelectorAll('.fixed').forEach(modal => {
    modal.addEventListener('click', (e) => {
      if (e.target === modal) {
        modal.classList.add('hidden');
      }
    });
  });
</script>
{% endblock %}